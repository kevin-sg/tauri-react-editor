import { HomePage } from '@/pages';

import type { FCC } from 'react';

const App: FCC = () => {
	return (
		<div className='w-full h-screen text-gray-100 font-poppins'>
			<HomePage />
		</div>
	);
};

export default App;
